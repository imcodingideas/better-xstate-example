export enum CounterStates {
  Active = 'active',
}

export enum CounterEvents {
  Increment = 'INCREMENT',
  Decrement = 'DECREMENT',
}

export enum CounterActions {
  Increment = 'increment',
  Decrement = 'decrement',
}
